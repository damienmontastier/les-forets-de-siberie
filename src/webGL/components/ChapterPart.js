import * as THREE from 'three'

//créé les layers
export default class ChapterPart extends THREE.Object3D {
  constructor() {
    super()
  }

  addMesh(layer, mesh) {
    //ajoute le mesh au layer spécifié
  }
}
